# Discovery notes

The previous exploratory phase reported the following finite checks, all with zero failures:

- 4,497 complete cases for `m = 3,4,6`;
- 18,672 general direct valuation checks;
- 8,203 structured cases;
- 566,934 independent coefficient comparisons between binomial valuations and a borrow counter;
- 10,374 independent theorem-level cases;
- separate checks of the repaired `p = m-1` uniform witness.

These numbers are provenance/evidence only, not proof.

The earlier archive was named `number_theory_discovery_bundle.zip`. It was not available on the active conversation/library file surfaces during the initial scaffold session, so exploratory scripts were not copied verbatim. The new `scripts/reference_check.py` is intentionally a clean proof-facing reimplementation.
